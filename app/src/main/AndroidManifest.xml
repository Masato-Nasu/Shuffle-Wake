<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Exact alarms (Android 12+). User may need to allow in system settings. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Notifications (Android 13+ runtime permission) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground playback -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Boot reschedule -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Help ensure CPU stays awake briefly during ring start -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Full-screen intent (required by policy when using alarm full-screen notifications on modern Android) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:label="@string/app_name">

        <activity
            android:name="com.example.shufflealarm.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.shufflealarm.ui.AlarmRingingActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTask" />

        <service
            android:name="com.example.shufflealarm.player.PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name="com.example.shufflealarm.alarm.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name="com.example.shufflealarm.alarm.AlarmActionReceiver"
            android:exported="false" />

        <receiver
            android:name="com.example.shufflealarm.alarm.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
